random_seed: 123

experiment_name: EDSR

sentinel_resolution: "20m"
planetscope_bands: "4b"

train: false
predict: true

model:
  name: edsr
  channels: 256
  blocks: 32

datamodule:
  batch_size: 32
  augment: True

trainer:
  max_epochs: 50
  profiler: "simple"
  log_every_n_steps: 5

optimizer:
  lr: 0.0002
  scheduler_step: 10